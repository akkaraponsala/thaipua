"""PySide6 widget panes composing the ThaiPUA desktop application.

Importing `thaipua.gui.widgets` does not itself require PySide6; only the individual
widget modules pull it. This keeps the parent `gui` subpackage importable without a
`QApplication`.
"""
